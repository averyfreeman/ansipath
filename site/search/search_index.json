{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ansipath \ud83c\udf08","text":"<p>Discover <code>$PATH</code> legibility: print each path on a new line in a rainbow of colors, so you can actually read it.</p> <pre><code>leg\u2022i\u2022bil\u2022i-ty | leja'bilade | noun\nthe quality of being clear enough to read: we've increased the type size for\ngreater legibility; design and layout clearly affect the legibility of the text.\nSyns: READABILITY, clarity, readableness, ease of reading\n</code></pre>"},{"location":"#features","title":"Features","text":"<ul> <li>Colorizes each <code>$PATH</code> entry by directory depth.</li> <li>Flags duplicate entries and directories that no longer exist.</li> <li>Includes <code>which</code> and <code>pathmunge</code> shell helpers for Bash and Zsh.</li> </ul>"},{"location":"#installation","title":"Installation","text":"<p>Install uv, then run these commands from the repository root:</p> <p>The default build uses <code>catppuccin_mocha</code>. To choose another palette, copy it to <code>palettes/palette.json</code> before building.</p> <pre><code>cp -v palettes/catppuccin_mocha.json palettes/palette.json\n./scripts/build.sh\nbash install_helpers.sh\n</code></pre> <p>The helper installer supports Bash and Zsh. Restart your shell, or source the profile file reported by the installer.</p>"},{"location":"#usage","title":"Usage","text":""},{"location":"#ansipath-command","title":"<code>ansipath</code> command","text":"<p>Print the current <code>$PATH</code>, one entry per line.</p> <pre><code>ansipath\n</code></pre> <p></p>"},{"location":"#which-helper","title":"<code>which</code> helper","text":"<p>Run <code>which</code> as usual. If a command is missing, the installed helper prints the lookup error and runs <code>ansipath</code> automatically.</p> <pre><code>which missing_command\n</code></pre> <p></p>"},{"location":"#pathmunge-helper","title":"<code>pathmunge</code> helper","text":"<p>Prepend, append, or remove a directory from <code>$PATH</code> in the current shell.</p> <pre><code>pathmunge prepend ~/.local/bin\n</code></pre> <p></p>"},{"location":"#development","title":"Development","text":"<pre><code>uv run mypy src/ tests/\nuv run pytest\n</code></pre>"},{"location":"installation/","title":"Ansipath manual installation","text":"<p>The helper installer supports Bash and Zsh on Unix-like systems. Run these commands from a shell, not Windows CMD or PowerShell.</p> <ol> <li>Confirm that the active shell is supported.</li> </ol> <pre><code>case \"${SHELL##*/}\" in\n    bash|zsh) ;;\n    *)\n        echo \"Error: install_helpers.sh supports Bash and Zsh only.\" &gt;&amp;2\n        exit 1\n        ;;\nesac\n</code></pre> <ol> <li>Check that <code>uv</code> is installed.</li> </ol> <pre><code>if ! command -v uv &gt;/dev/null 2&gt;&amp;1; then\n    echo \"Error: 'uv' package manager was not found.\" &gt;&amp;2\n    echo \"Install uv first: https://docs.astral.sh/uv/getting-started/installation/\" &gt;&amp;2\n    exit 1\nfi\n</code></pre> <ol> <li>Select the palette and install ansipath as a global <code>uv</code> tool from the repository root. The build script copies the selected palette into the package configuration before invoking <code>uv tool install</code>.</li> </ol> <pre><code>cp -v palettes/catppuccin_mocha.json palettes/palette.json\n./scripts/build.sh\n</code></pre> <p>Restart the terminal, or source the profile file reported by the installer.</p>"}]}